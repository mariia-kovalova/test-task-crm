- const search = customers.search;
- const list = customers.list; 
- const pagination = customers.pagination; 

section.customers
  .wrapper
    .customers__content
      .customers__top
        div
          h1.customers__title #{customers.title}
          p.customers__subtitle #{customers.subtitle}
        include ../components/search
      include ../components/customers-list
      include ../components/pagination